server:
  port: ${PORT:8080}

spring:
  application:
    name: medai-pptx-renderer
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  jackson:
    default-property-inclusion: non_null

logging:
  level:
    com.medai: INFO
    org.apache.poi: WARN
